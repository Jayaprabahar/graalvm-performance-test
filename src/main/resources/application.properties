spring.application.name=graalvm-performance-test

# Default MongoDB connection (change to your MongoDB URI as needed)
spring.data.mongodb.uri=mongodb://localhost:27017/graalvm_performance_test

# Expose actuator endpoints for metrics and health
management.endpoints.web.exposure.include=health,metrics
